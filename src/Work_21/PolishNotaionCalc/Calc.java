package Work_21.PolishNotaionCalc;

import java.util.Stack;

public class Calc extends Stack<Character> {

//    public Integer parse(String parseData) {
//        for (int i = 0; i < parseData.length(); i++) {
////            switch (parseData.charAt(i)) {
////                case '+': this.push(this.pop() + this.get())
////            }
//        }
//
//    }
}
